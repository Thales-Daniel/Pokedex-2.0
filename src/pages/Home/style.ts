import styled from "styled-components"
import background from "../../shared/images/poke-background.png"

const Main = styled.main`
  display: flex;
  align-items: center;
  justify-content: center;
  background-image: url(${background});
`
export default Main
